box: feaster83/java8-oracle@1.0.2

build:
  steps:
    - arjen/hugo-build:
      version: "0.14"
      theme: herring-cove
      flags: --buildDrafts=true

deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
      token: $GIT_TOKEN
      domain: egeek.co
      basedir: public
      repo: egeek.github.io
